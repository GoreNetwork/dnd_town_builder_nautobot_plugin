"""Urls for robot_platform_data."""

from django.urls import path

from robot_platform_data import views

urlpatterns = [
    # path('random/', views.RandomAnimalView.as_view(), name='random_animal'),
]